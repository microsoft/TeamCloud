tc user update:
  parameters:
    base_url:
      rule_exclusions:
      - no_parameter_defaults_for_update_commands
tc project user update:
  parameters:
    base_url:
      rule_exclusions:
      - no_parameter_defaults_for_update_commands